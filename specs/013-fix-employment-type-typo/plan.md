# Implementation Plan: Fix `employmentType` Typo in Workforce API

**Branch**: `013-fix-employment-type-typo` | **Date**: 2026-04-16 | **Spec**: [spec.md](spec.md)  
**Input**: Feature specification from `specs/013-fix-employment-type-typo/spec.md`

## Summary

Rename the misspelled `employementType` field to `employmentType` across 7 source locations and 4 test locations. This is a pure identifier rename: no logic changes, no new files, no new dependencies. All changes are in `src/types/workforceTypes.ts` (type definition), `src/store/slices/workforceSlice.ts` (static data fixture), `src/hooks/useWorkforceDemographicsConfig.ts` (three property accesses), `src/store/selectors/workforceSelectors.ts` (JSDoc comment), and three test fixture objects.

## Technical Context

**Language/Version**: TypeScript 5.x (strict mode)  
**Primary Dependencies**: Redux Toolkit, React 19 + Vite  
**Storage**: N/A (no persistence changes)  
**Testing**: Vitest + React Testing Library (`pnpm run test`)  
**Target Platform**: Web (Vite SPA, client-side)  
**Project Type**: Single web application  
**Performance Goals**: N/A (no runtime behaviour changes)  
**Constraints**: Zero TypeScript errors after rename; zero test regressions  
**Scale/Scope**: 7 source file edits, 4 test file edits; no new files created

## Constitution Check

_GATE: Must pass before Phase 0 research. Re-checked after Phase 1 design._

| Principle                       | Status  | Notes                                                             |
| ------------------------------- | ------- | ----------------------------------------------------------------- |
| I. Component-First Architecture | ✅ Pass | No component changes; type rename only                            |
| II. User-Centric Design         | ✅ Pass | Spec has user stories + acceptance criteria                       |
| III. Test-Driven Development    | ✅ Pass | Existing tests updated in same PR; no new untested logic          |
| IV. Type Safety & Code Quality  | ✅ Pass | Strict-mode types enforced; rename eliminates misleading spelling |
| V. Performance & Accessibility  | ✅ Pass | No UI changes; no accessibility impact                            |
| VI. State Management Discipline | ✅ Pass | Redux shape unchanged; only field key renamed                     |

No violations — Complexity Tracking table not required.

## Project Structure

### Documentation (this feature)

```text
specs/013-fix-employment-type-typo/
├── plan.md         ← this file
├── research.md     ← Phase 0 output
├── data-model.md   ← Phase 1 output
├── quickstart.md   ← Phase 1 output
├── contracts/
│   └── workforce-get-updated.md  ← corrected contract excerpt
└── tasks.md        ← Phase 2 output (generated by /speckit.tasks)
```

### Source Code (affected files only)

```text
src/
├── types/
│   └── workforceTypes.ts               # FR-001, FR-002  — rename field + remove @note
├── store/
│   ├── slices/
│   │   └── workforceSlice.ts           # FR-003          — rename key in STATIC_WORKFORCE_DATA
│   └── selectors/
│       └── workforceSelectors.ts       # FR-005          — update JSDoc comment
└── hooks/
    └── useWorkforceDemographicsConfig.ts  # FR-004       — three property accesses

tests/
├── store/
│   ├── workforceSlice.test.ts          # FR-006  — two fixture objects (lines 71, 134)
│   └── workforceSelectors.test.ts      # FR-006  — one fixture object (line 49)
└── services/
    └── workforceApi.test.ts            # FR-006  — one fixture object (line 64)
```

**Structure Decision**: Single web-application layout. No new directories created. All edits are in-place identifier renames within existing files.
